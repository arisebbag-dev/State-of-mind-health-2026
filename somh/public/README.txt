ASSETS IN THIS BUILD
====================
nechama.jpg — IMPORTANT: the nechama.jpg currently in this /public folder is a
BLANK PLACEHOLDER (solid color) just so the build works. REPLACE IT with the
real headshot, same filename: nechama.jpg. The code already displays it in the
hero, the "Meet Nechama" section, and the About page — no code changes needed,
just overwrite the file.

og-image.jpg — optional 1200x630 social share image.

favicon — drop favicon.ico or icon.png into the /app folder.

TESTIMONIALS
============
components/Testimonials.jsx has SAMPLE quotes for preview only.
Replace with real, patient-approved testimonials before launch.

INTAKE FORM
===========
components/IntakeForm.jsx shows the callback confirmation but does not yet
send anywhere. Wire the submit to email/Formspree/your EHR (comment marks the spot).
